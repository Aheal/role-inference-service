services:
  api:
    build: .
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: "file:/data/dev.db"
      PORT: "3000"
    volumes:
      - sqlite-data:/data

volumes:
  sqlite-data:

